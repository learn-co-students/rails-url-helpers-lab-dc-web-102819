<div>
  <% @students.each do |student| %>
    <% if student.active %>
  <p>  <%= student.to_s + ":  This student is currently active." %></p>
<% else %>
  <p>  <%= student.to_s + ":  This student is currently inactive." %></p>
<% end %>
  <% end %>
</div>

