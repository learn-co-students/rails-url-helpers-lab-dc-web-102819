
<div>
    <% if @student.active %>
        <h1> <%= @student.to_s + ":  This student is currently active." %></h1>
    <% else %>
        <h1> <%= @student.to_s + ":  This student is currently inactive." %></h1>
    <% end %>
</div>
